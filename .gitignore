# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Jupyter
.ipynb_checkpoints/

# Raw Kaggle download: 640 MB of CSV plus a linux-only .so. Redownloadable from
# the competition page, never edited, and nothing at runtime reads it — only
# scripts/build_fixture.py does, once. See CLAUDE.md -> Data policy.
data/raw/

# Model artifacts
models/
*.txt.model
*.pkl

# Scratch
.DS_Store
scratch/
runs/

# Built fixture: 130 MB, rebuilt from data/raw/ by scripts/build_fixture.py in
# seconds (the run records its own `build_seconds` in the manifest), pinned by the
# committed manifest.json (row counts + sha256). The 4 MB smoke fixture beside
# it IS committed so the test suite runs on a fresh clone. See CLAUDE.md.
data/fixtures/train.parquet
